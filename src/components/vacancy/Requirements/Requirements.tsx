import { FC } from 'react';
import Card from './Card';

const Requirements: FC = () => {
  return (
    <div className='w-full grid grid-cols-3 pt-[40px] gap-[13px]'>
      <Card />
    </div>
  );
};

export default Requirements;
